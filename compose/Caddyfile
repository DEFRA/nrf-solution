:4001 {
    # Route upload requests to cdp-uploader
    handle /upload-and-scan/* {
        reverse_proxy cdp-uploader:7337
    }

    # Route everything else to backend on the shared Docker network
    handle {
        reverse_proxy backend:3001
    }
}
